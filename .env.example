# Required
DATABASE_URL=
NEXTAUTH_SECRET=
NEXTAUTH_URL=

# Public deployment configuration
NEXT_PUBLIC_APP_URL=
NEXT_PUBLIC_APP_NAME=
APP_MODE=production

# Optional tenant binding
SINGLE_BUSINESS_ID=
OWNER_REGISTRATION_MODE=onboarding

# Optional SMTP/email (password reset, notifications, contact)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=

# Optional Twilio SMS
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=

# Optional Google Business Profile integration
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GBP_ACCESS_TOKEN=
GBP_REFRESH_TOKEN=
GBP_ACCOUNT_ID=
GBP_LOCATION_ID=

# Optional scheduled reminders
CRON_SECRET=

# Optional test-only endpoint flag; never enable for normal client deployments
ENABLE_E2E_TESTS=false

# Client business name, contact details, address, services, staff, hours, timezone,
# and policies belong in the Business and related database records through onboarding.
# BUSINESS_TIMEZONE is intentionally not an environment variable.
