{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "if [ -z \"$DATABASE_URL\" ] && [ -n \"$NEON_POSTGRES_PRISMA_URL\" ]; then export DATABASE_URL=\"$NEON_POSTGRES_PRISMA_URL\"; fi; prisma migrate deploy && prisma generate && next build",
  "installCommand": "npm install",
  "crons": [
    {
      "path": "/api/cron/reminders",
      "schedule": "0 9 * * *"
    }
  ]
}
